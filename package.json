{
  "name": "smart-grocer-beta-website",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit && tsc -p functions/tsconfig.json --noEmit",
    "pages:dev": "npm run build && wrangler pages dev dist --compatibility-date=2026-08-17",
    "preview": "npm run pages:dev"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "latest",
    "typescript": "^5.9.2",
    "vite": "^7.1.3",
    "wrangler": "^4.28.1"
  }
}
